# Source snapshot provenance

This import was read directly from AppDeploy application `globespark-ai-v11dlz`, applied snapshot `1789447100589`, on 2026-09-15.

The import intentionally excludes private prospecting workbook contents, secrets, credentials and paid connector outputs.

Runtime QA for the applied production deployment reported 0 frontend errors and 0 network errors; AppDeploy did not surface an automated E2E run (`e2e_tests = null`), so no E2E-pass claim is made.
